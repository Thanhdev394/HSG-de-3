#include <bits/stdc++.h>


using namespace std;
typedef long long ll;
ll n,m;
signed main()
{
    freopen("bai1.inp","r",stdin);
    freopen("bai1.out","w",stdout);
    cin >> n >> m;
    cout << (n+1)*m + (m+1)*n;
    return 0;
}
