#include <bits/stdc++.h>
#include <math.h>
#include <iostream>
using namespace std;
int n, m;
int main()
{
    freopen("HANGRAO.inp", "r", stdin);
    freopen("HANGRAO.out", "w", stdout);
    cin >> m >> n;
    cout << m * 2 * m + n * n;
    return 0;
}
